export interface DPR{
    date: string
    work_description: string
    weather: string
    worker_count: number
}

export interface RDPR{
    date: string
    work_description: string
    weather: string
    worker_count: number
    project_id: string
    user_id: string
}